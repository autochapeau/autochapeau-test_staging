<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_pos_pos_form_vehicle" model="ir.ui.view">
        <field name="name">pos.order.form.vehicle</field>
        <field name="model">pos.order</field>
        <field name="inherit_id" ref="point_of_sale.view_pos_pos_form"/>
        <field name="arch" type="xml">
            <field name="partner_id" position="attributes">
                <attribute name="readonly">state != 'draft'</attribute>
            </field>
            <field name="partner_id" position="after">
                <field name="vehicle_id" readonly="state != 'draft'"/>
            </field>
        </field>
    </record>

    <record id="view_pos_order_tree_vehicle" model="ir.ui.view">
        <field name="name">pos.order.tree.vehicle</field>
        <field name="model">pos.order</field>
        <field name="inherit_id" ref="point_of_sale.view_pos_order_tree"/>
        <field name="arch" type="xml">
            <field name="partner_id" position="after">
                <field name="vehicle_id" optional="show"/>
            </field>
        </field>
    </record>
</odoo>
