<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="pos_partner_car.CarSelectionPopup">
        <div class="popup popup-selection">
            <div class="modal-header drag-handle">
                <h4 class="modal-title"><t t-esc="props.title"/></h4>
            </div>
            <div class="selection btn-group-vertical justify-content-start w-100 p-3 overflow-y-auto">
                <t t-if="!props.list.length">
                    <div class="text-muted text-center py-3">
                        <t t-esc="props.emptyText"/>
                    </div>
                </t>
                <t t-foreach="props.list" t-as="item" t-key="item.id">
                    <button type="button"
                            class="selection-item d-flex align-items-center justify-content-between btn btn-lg btn-outline-secondary w-100 p-3 text-start"
                            t-att-class="{ 'selected active': item.isSelected }"
                            t-on-click="() => this.selectItem(item.id)">
                        <div class="d-flex flex-column">
                            <span t-esc="item.label"/>
                        </div>
                        <i class="oi oi-chevron-right"/>
                    </button>
                </t>
            </div>
            <footer class="footer modal-footer gap-2">
                <div class="button cancel btn btn-lg btn-primary" t-on-click="cancel">
                    <t t-esc="props.cancelText"/>
                </div>
                <div class="button btn btn-lg btn-secondary" t-on-click="createNew">
                    <t t-esc="props.newText"/>
                </div>
            </footer>
        </div>
    </t>
</templates>
